# 独立复核修复实施计划

> 使用工作流内置 executing-plans 串行完成，不启动新的子 agent。
> 每项遵循失败测试 → 最小实现 → 定向验证，末尾完整回归。

**目标：**修复复核报告 12 项确定问题、2 项补充边界及已确认死代码，README 正确归档并提交推送。
**架构：**保持现有文本块、位置写回和还原；局部复用上游表达式及现有依赖。
**环境：**Windows、本机 Python 3.14、本机 Excel、PyMuPDF、python-docx、openpyxl。
**设计：**docs/superpowers/specs/2026-09-19-独立复核修复-design.md。
**实际仓库：**C:\Users\27651\Documents\Codex\2026-09-19\https-github-com-quanfanpro-code-local\work\local-doc-desensitizer。

## 六阶段与约束

- [x] 阶段 1：据用户明确指令、报告与原需求确认范围。
- [x] 阶段 2：沿用报告方向，比较三方案，选局部复用。
- [x] 阶段 3：写入本计划，顺序及验收标准确定。
- [x] 阶段 4：按下列任务逐项修复验证。
- [x] 阶段 5：验收审查。
- [x] 阶段 6：提交推送并验证远程。

所有已有文件修改前已备份校验；新增中文文件 UTF-8 BOM，解析格式例外。不得永久删除文件。无新运行依赖，无用户认证信息落盘；不更改不相关项目。

## 任务与预期

1. 固定规则与合并：修改 主程序/固定规则.py、ner引擎.py、识别结果.py；新增 开发测试/test_独立复核修复.py。断言“银行账号：123456789012”含完整银行账号、金额列数字1234567.89变为100万余元、MAC不吞标签、中文小写车牌完整命中；负例数量/编号不改；错误模型范围不留下38000；分隔身份证保留原跨度，无效日期无命中。
2. Word 和 Excel：修改 主程序/_docx_xml工具.py、文档解析器.py、定位写回.py；同一新测试模块覆盖脚注尾注与公式未执行分支，保存后的XML/公式不含号码，普通公式引用保留，还原原公式和节点。
3. PDF：修改 主程序/文档解析器.py、定位写回.py、脱敏处理器.py；测试扫描OCR文字层分流至MD、邻行Ordinary text stays不变、输出和还原均保留未改文字。已有图背景测试继续通过。
4. GUI：修改 主程序/app.py；使用真实方法与处理器，仅控件和不可用服务替身，断言文字PDF纯正则零模型调用、扫描混合批次可继续普通TXT，取消还原显示取消且只有已处理数量。
5. 清理与文档：移除报告79行无调用定义；复制相关上游LICENSE并记录固定提交和适配；更新CONTEXT中的公式文字常量术语；README本轮记录仅写入已有更新模块，正文同步实际能力。
6. 验收交付：原授权真实在线模型复测；实际Office/PDF/还原核验；全部测试与解析/差异/密钥检查；新增验收报告；提交、快进同步main、推送origin main并核对远端提交。

各任务测试使用同一正式保留材料入口，工作目录为上列实际仓库：
C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe -B -X utf8 维护工具\运行脱敏验证.py --module 开发测试.test_独立复核修复
任务完成再按受影响模块验证，最终使用 --all；预期退出0且全部通过。新增失败测试须先以原实现运行并记录实际失败，不通过不得提交推送。

## 任务状态

- [x] 1 固定规则与合并
- [x] 2 Word 与 Excel
- [x] 3 PDF
- [x] 4 GUI
- [x] 5 清理与文档
- [x] 6 验收交付

## 执行与验收记录

规则、Office、PDF、GUI 均先复现失败再修复。验收另查出无冒号账号兼容及金额字段对后续数量的误改，先补两项失败测试再修复；最终 146 项通过，退出码 0。真实 Qwen 复测 3 份材料，12 次调用，全部准确还原；2 份完整，1 份因模型空结构如实报告部分完成。5000 公式完整解析（含 5000 文字常量）单次 2.9612 秒，原件哈希不变。PDF 原件、脱敏和还原渲染已目视核对。

用户已明确授权提交推送，不重复确认；修复提交 ed629ee89855caca7caa8508ab041b8a6e7d974d 已快进同步并推送 origin/main，远端查询与本地提交完全一致。未新增依赖或 agent，未删除文件、分支或工作区。

提交前检查：39 个 Python 文件语法通过、75 个项目文件未发现用户密钥、修改文件中文无替换字符、README 两个日期各仅出现于原更新模块，文档相对链接及许可证哈希通过。当前补记仅更新实施计划，不改变已验证业务代码。
