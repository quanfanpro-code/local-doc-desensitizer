# 全面复核修复 — 实施计划（2026-07-25）

对应需求：docs/requirements/2026-07-25-全面复核-requirement.md

## 任务清单（每项 = 先写测试 → 再实现 → 再验证）

- [x] [1] 备份：复制整个项目到 `C:\Users\27651\BackUp\本地文档脱敏工具_20260725\`
- [x] [2] 写 扫描版PDF脱敏文本保留 测试 → 修复：脱敏处理器接住 `文本脱敏()` 返回值；`生成脱敏md` 增加可选的「已有文本」参数，扫描版 PDF 直接用脱敏后的 OCR 文本写 md → 验证
- [x] [3] 写 调试日志开关+异常关闭 测试 → 修复：调试日志改为 settings.json `debug_ner_log` 开关（默认关），并用 try/finally 保证关闭 → 验证
- [x] [4] 写 注册自定义映射超10次碰撞 测试 → 修复：①~⑩用完后回退到 `(11)(12)...` 数字后缀，绝不静默放弃 → 验证
- [x] [5] app.py 进度条：去掉 `*100`，改传 0~1 小数（无测试，GUI 逻辑，目检）
- [x] [6] app.py 开始脱敏前的 LLM 检查：按 llm_mode 分别探测本地 LM Studio / 在线 API；扫描版 PDF 改查 MinerU 是否可用（无测试，GUI 逻辑，目检）
- [x] [7] 脱敏处理器.py / 格式保持器.py：包导入加 try/except 兜底，支持直接运行 `python 主程序/app.py` → 验证导入
- [x] [8] 环境自检.py：去掉 ltp、补上 ahocorasick → 运行验证
- [x] [9] app.py `_检测lm状态`：Tkinter 变量在主线程取值后再传入后台线程（目检）
- [x] [10] ner引擎.py NER 跳过警告文案按模式区分本地/在线（顺手）
- [x] [11] 跑全部测试 `python -m pytest 开发测试 -v` → 全绿
- [x] [12] 更新 readme.md（扫描版 PDF 前提、调试日志开关、更新记录）
