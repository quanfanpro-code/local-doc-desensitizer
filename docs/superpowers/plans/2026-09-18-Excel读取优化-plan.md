# Excel 读取优化实施计划

> 执行者使用工作流内置 executing-plans 串行实施；用户未要求子 agent。六阶段检查点由用户“完全自主决策”覆盖，交付为 main 提交和推送。

**目标：**保持公式脱敏与还原正确，减少批量读取耗时。

**结构：**单任务惰性 Excel 会话、有限区域 Value2 批量取值、解析与输出复查共用。

**技术：**本机 Python 3.14、Excel、pywin32、openpyxl，不增加依赖。

**方案：**[设计](../specs/2026-09-18-Excel读取优化-design.md)，[需求](../../requirements/2026-09-18-Excel读取优化-requirement.md)。

## 约束与六阶段

仅 Windows；修改前备份并校验；原件只读；不删除材料；不开分支；沿用原 GUI；不改模型。既有机构检测改进一并交付。

- [x] 阶段 1：需求及追问结论
- [x] 阶段 2：方案比较及领域术语
- [x] 阶段 3：实施计划，自查覆盖全部验收标准
- [x] 阶段 4：TDD 编码
- [x] 阶段 5：审查及新鲜验证
- [ ] 阶段 6：提交并推送 main

仓库内路径均相对于项目根目录。验证命令的 Python 为 `C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe`，参数为 `-B -X utf8 维护工具\运行脱敏验证.py`。

## 任务 1：公式批量读取与会话生命周期

文件：新增 `主程序/Excel读取.py`、`开发测试/test_Excel读取优化.py`；修改 `主程序/文档解析器.py`。

接口：`Excel公式读取会话(取消检查=None)` 上下文管理器，`读取(路径, 单元格列表) -> dict`，`问题 -> list[str]`；`读取Excel公式值(路径, 单元格列表, 会话=None) -> dict` 保持双参数可用。

- [x] RED：5000 个连续公式、相距极远的公式的结果及单次区域上限，零/False/空文本/错误不混淆，连续读取和异常后释放；运行 `--module 开发测试.test_Excel读取优化`，缺少会话及真实入口启动 4 次而失败。
- [x] GREEN：一次打开及重算，分列有限范围读取，在 finally 中关闭；退出只清理本任务实例，无公式不启动。
- [x] VERIFY：定向测试通过，本机 Excel 混合结果与源哈希正确。

```python
with Excel公式读取会话() as 会话:
    值 = 会话.读取(路径, [("数据", "A1"), ("数据", "A2")])
assert 值[("数据", "A1")] == "星河"
assert 值[("数据", "A2")] == 0
```

## 任务 2：解析及整批处理接入

文件：`主程序/文档解析器.py`、`主程序/脱敏处理器.py`、上述新测试。

接口：`读取文档块(文件路径, Excel会话=None)`；公开 `处理文件列表` 签名不变，内部传递同一会话。

- [x] RED：稀疏表不填充空格；上下文用计算值；带旧缓存或输出复算失败必须记录；真实两文件脱敏及重读只启动一个 Excel，原件不变且映射可恢复。
- [x] GREEN：按已有单元格组织行；原值与检测文字分开；原件、输出和补做后读取传入会话；资源问题与复算失败进入未完成说明。
- [x] VERIFY：定向模块及 `--module 开发测试.test_输出完整性 --module 开发测试.test_文档解析器` 共 29 项全通过，新增故障检查后全量共 124 项通过。

```python
块 = 读取文档块(路径, Excel会话=会话)
assert 块[0].原文 == "星河"
assert 块[0].原值 == '= "星河"'
```

## 任务 3：效率复测与说明

文件：新增 `维护工具/验证Excel读取效率.py`；更新 README、CONTEXT 与执行记录。

- [x] 使用正式函数读取既有合成数据，核对全部公式值与源哈希；8/1000/5000 公式及 10 文件各 3 轮、中位数，对比已保存旧实现证据。
- [x] 明确测试只测读取阶段；真实入口脱敏、复查及还原单独验证，不能用模型时间掩盖读取性能。
- [x] 文档写明复用、分块、错误/缓存、依赖与原生调用期间取消限制。

## 任务 4：审查和仓库交付

- [x] 按工作流阶段 5 检查正确性、数据保护、错误释放和不必要复杂度；发现缺陷先补失败测试再修复。
- [x] `--all` 返回 0；源码 AST、JSON、中文编码、敏感配置和 diff 检查通过；记录最终源文件哈希与新鲜证据。
- [x] 更新上一轮 Git 交付待定状态，完整保留上一轮测试及模型测量，不把旧模型实测称为本轮重跑。
- [ ] 重新核对路径、main 和 origin；正常提交、推送，不强推；远端 main 与本地 HEAD 一致且工作区干净。最终报告结果、性能、测试及提交链接。
