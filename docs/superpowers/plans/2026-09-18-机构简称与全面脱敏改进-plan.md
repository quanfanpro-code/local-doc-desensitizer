# 机构简称与全面脱敏改进实施计划

> 执行者：按需求优先工作流内置的 executing-plans.md 逐项串行执行。用户未授权子 agent；不启动并行开发。每项完成失败测试、最小实现和范围内全量验证，再进入下一项。

**目标：**提高机构及任意简称的检测、关联和逐处替换能力，同时完成既有其他敏感类别、实际文件输出、复查及准确还原。

**架构：**采用用户选定的两轮识别：先整理当前文件的机构资料，再结合这些资料逐段检查全部原文。统一保存原文位置、机构关系和原始写法；全称 [公司1]、简称 [公司1]①。规则提供固定格式识别和候选补充，不限制任意简称进入模型。

**技术：**Windows、本机 Python 3.14、现有 requests、pyahocorasick、python-docx、openpyxl、python-pptx、PyMuPDF；本机 Excel 与已安装 pywin32 用于公式计算值读取。已安装 phonenumbers 9.0.39。

**设计依据：**[已明确的方案][设计]、[需求及用户纠正][需求]、[机构代号决策][代号决策]。本文件状态：六阶段完成；后续用户授权的 Excel 优化一起进入功能提交 3e7bac9，已推送现有 main 并核对远端一致。下文早期分支及未授权约束仅记录当时计划，以此后续授权为准。

## 全局约束与执行边界

- 目标克隆固定为 C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer；当前基线 576e2668c351d113d2f0da4322154652c00f3aee。它是本任务单独克隆，未修改用户其他检出目录。
- 批准后在此克隆建立本地分支 codex/org-alias-detection，不另建工作树，不改其他仓库；先检查根目录、状态并 fetch origin，远端若变动只评估相关差异，不自动覆盖本次文档。
- 修改已有文件前，备份到 C:\Users\27651\BackUp\local-doc-desensitizer_年月日_时分秒，保持相对路径并核对哈希。所有测试材料保留在任务的 work 目录；现有测试临时目录清理要在验证入口中改为保留，不执行永久删除。
- 中文文件 UTF-8，普通新文档及中文数据使用 BOM；源码按现有 UTF-8 约定并用指定解释器验证。不记录 API Key，不把运行设置或样例原始业务资料加入源码。
- 逐文件独立，不跨文件共享机构、代号或上下文。确定是机构但全称未知的简称仍独立脱敏。
- 不重复运行相同正则充当补漏；模型漏检改上下文及识别方法；仅临时连接等可恢复请求错误有限重试。
- 即使某个环节未完整，仍交付已处理文件及对应还原映射，说明未完成环节；不增设逐词人工确认、隔离目录或放行流程。
- 日期、金额保留现有启用方式；其他既有敏感类别不能因优先级低被删减。
- 本计划包含安装 phonenumbers==9.0.39 和记录 pywin32 的 Windows 依赖。其余现有依赖可用，不全量升级。源码、测试、README 和来源说明在本次范围内；不自动提交、推送、建 PR 或打包发布。

## 文件与职责

| 文件 | 调整内容 |
|---|---|
| [识别结果.py][结果]（新增） | 原文块、出现记录、识别结果及按位置替换，不另建通用框架 |
| [文档解析器.py][解析]、[_docx_xml工具.py][Word工具] | 保留格式位置、表格上下文、数字类型和公式显示值；Word 读取与写回使用一致文本节点集合 |
| [ner引擎.py][NER] | 两轮机构与简称识别、结构解析、上下文关联、逐处核对；升级映射且兼容旧格式 |
| [固定规则.py][规则]（新增）、[词典.json][词典] | 既有固定格式规则、校验及冲突处理；词典可追溯且不否决上下文实体 |
| [格式保持器.py][写回] | TXT、MD、Word、Excel、PPT、PDF 按位置写回与还原 |
| [脱敏处理器.py][处理器]、[app.py][界面] | 共用文件流程、保存后复查、输出与完成程度分开、取消和进度 |
| [运行脱敏验证.py][验证入口]（新增） | 在保留材料的前提下执行指定测试模块或本项目全部离线测试 |
| [test_机构识别改进.py][机构测试]（新增） | 两轮、跨段、无规律简称、普通词同形、关系和映射回归 |
| [test_输出完整性.py][输出测试]（新增） | 各格式实际文件、原文还原、公式、保存及异常输出回归 |
| [test_固定规则.py][规则测试]（新增） | 所有既有敏感类别的正例、边界和误报回归 |
| [机构识别标注.json][标注]（新增）、[验证机构识别.py][模型验证]（新增） | 固定虚构标注材料、改前改后真实模型比较及指标 |
| [requirements.txt][依赖]、[规则来源.md][来源]（新增）、[README.md][说明] | 最小依赖、上游许可和规则采用说明、真实行为及验证范围 |

## 一致的数据接口

这些是计划接口，实施时先以行为测试约束，必要内部调整须同步所有调用者，不能让新旧流程分别按位置和全局名称替换。

```python
from dataclasses import dataclass

@dataclass
class 文本块:
    编号: str
    原文: str
    位置: dict  # 部件/段落，或工作表/单元格，或幻灯片/形状，或 PDF 页/文字区域
    上下文: str = ""
    原值: object = None
    数据类型: str = "text"

@dataclass
class 出现记录:
    块编号: str
    开始: int
    结束: int
    原词: str
    类型: str
    机构编号: str | None = None
    来源: str = "模型"

@dataclass
class 识别结果:
    机构: dict  # 机构编号 -> {全称或 None, 原文称呼列表, 关系证据}
    出现: list[出现记录]
    已核对块: set[str]
    未完成: list[dict]  # 块编号、处理环节、实际原因；不编造漏词清单
```

模块函数接口：按位置替换(原文: str, 替换: list[tuple[int, int, str]]) → str，由任务 6 实现；读取文档块(文件路径: str) → list[文本块]，由任务 2 实现；固定规则识别(块: 文本块, 启用日期: bool, 启用月日: bool, 启用金额: bool) → list[出现记录]，由任务 7 实现。

NER引擎 新入口为 识别文档(块列表, 进度回调=None) -> 识别结果；现有 文本脱敏 接口保留为单文本块适配。映射新增 注册机构称呼(机构编号, 原文, 子类型, 是否全称=False) -> str；以“机构编号＋原文写法”区分同名简称。格式保持器 新入口为 保存定位脱敏(原路径, 块列表, 识别结果, 映射, 输出目录) -> str；旧接口保留兼容，但正式处理器与 MD 输出全部改用定位结果。

## 通用测试命令与判断方式

每次 PowerShell 先设置 UTF-8。以下变量在同一调用内声明；模块名字由各任务给出，程序根目录始终是上述目标克隆。

```powershell
$OutputEncoding = [Console]::OutputEncoding = [Text.Encoding]::UTF8
$项目 = 'C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer'
$解释器 = 'C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe'
& $解释器 -B -X utf8 "$项目\维护工具\运行脱敏验证.py" --module 开发测试.test_机构识别改进
```

--module 可重复指定，运行这些模块的全部用例；--all 发现本仓库开发测试目录的全部离线用例。每项红灯必须来自所要修复的行为，不能把解释器路径错误算红灯。绿灯为退出码 0、全部相应用例通过，无意外网络请求、原文件哈希一致。模型验证独立运行，不能把 mock 通过称为模型通过。

下面代码是各任务要落入 unittest 的核心断言和实现算法，文件级装配在对应任务完成。测试中的文件路径统一在任务 1 的保留目录内创建；工作簿、Word/PPT/PDF 均由相应已安装库生成虚构材料，不依赖用户文件。每项先运行新增测试确认行为失败，再实现所列代码，最后运行该项列出的全部模块；不能只写断言而不执行。

## 任务 1：建立可保留证据的验证入口及改前基线

修改或新增：[验证入口][验证入口]、[输出测试][输出测试]；读取现有开发测试，不改其预期来掩盖失败。

- [x] 失败测试：临时目录退出后材料必须仍可读取，构造一个失败用例时入口必须返回非零。用子进程调用入口，断言生成文件在上下文退出后存在。

```python
with 验证用临时目录(本任务work目录) as p:
    原文路径 = Path(p) / "样例.txt"
    原文路径.write_text("星河", encoding="utf-8-sig")
self.assertTrue(原文路径.exists())
```

- [x] 最小实现：入口内部将 TemporaryDirectory 定向到 work 下的独立测试材料目录，cleanup 只关闭句柄并保留目录；对受测失败清理分支拦截删除并记录路径。返回 unittest 的真实退出状态。实现保留目录的辅助函数只放在验证入口，不扩散到生产代码。

```python
from contextlib import contextmanager
from pathlib import Path
from tempfile import mkdtemp

@contextmanager
def 验证用临时目录(根目录: Path):
    根目录.mkdir(parents=True, exist_ok=True)
    路径 = Path(mkdtemp(prefix="脱敏验证_", dir=根目录))
    try:
        yield 路径
    finally:
        print(f"验证材料保留：{路径}")
```

上述失败测试传入本任务 work 目录。兼容旧测试的 TemporaryDirectory 替代类也调用此函数，并提供 name、__enter__、__exit__ 和保留目录的 cleanup；不改动系统标准库文件。
- [x] 保存 HEAD 的源码快照至本任务 work 下的新基线目录，用于最后相同样例比较；不包含密钥与用户设置。建立本地实施分支并 fetch，记录根目录、基线和状态。
- [x] 验证：入口自检后执行 --all，记录既有失败和原因；既有缺陷留为待修复证据。不得将旧 README 的测试数量当本轮结果。

## 任务 2：保留原文块、来源位置和表格上下文

新增 [识别结果][结果]，修改 [解析器][解析]、[Word 工具][Word工具]；测试 [输出测试][输出测试]。

- [x] 失败测试：同一个 Word run 内两个文字节点均可读取；Excel 数字型手机号、纯数字文本和含金额的机构单元格不丢失；多级表头可供上下文引用。

```python
块 = 读取文档块(str(含数字和机构的工作簿))
self.assertTrue(any(x.位置.get("单元格") == "B2" and "13800138000" in x.原文 for x in 块))
self.assertTrue(any("成都星河科技有限公司" in x.原文 for x in 块))
self.assertTrue(any("供应商" in x.上下文 for x in 块))
```

- [x] 最小实现：按格式生成稳定块编号，保留全部原始值与位置；只对送给模型的视图做整理。Word 统一收集全部 w:t、表格及当前支持部件；Excel 保留 sheet/cell/type，PPT 保留页/形状/段落，PDF 保留页和文字位置。扫描输出沿用已有 OCR 文本，保留可得的页或块信息，不伪造原 PDF 坐标。
- [x] 核心结构赋值：文本块(编号="xlsx:0:B2", 原文="13800138000", 位置={"工作表": "Sheet1", "单元格": "B2"}, 上下文="手机号", 原值=13800138000, 数据类型="n")。旧 提取文本 以块原文组合维持兼容。

```python
# 先生成上述失败测试的输入，变量来自保留目录内的样例路径。
from openpyxl import Workbook
工作簿 = Workbook()
工作表 = 工作簿.active
工作表.append(["供应商", "手机号"])
工作表.append(["成都星河科技有限公司", 13800138000])
工作簿.save(含数字和机构的工作簿)
# 解析器的单元格循环保留原值；公式显示值由任务 9 补齐。
单元格 = 工作表["B2"]
数字块 = 文本块("xlsx:0:B2", str(单元格.value),
    {"工作表": 工作表.title, "单元格": 单元格.coordinate},
    上下文="手机号", 原值=单元格.value, 数据类型=单元格.data_type)
```
- [x] 验证：--module 开发测试.test_输出完整性 --module 开发测试.test_文档解析器；逐种格式核对块来源可逆对应，不把多个单元格拼成无法定位的整行。

## 任务 3：机构关联与原文写法分别保存

修改 [NER 与映射][NER]；测试 [机构测试][机构测试]及现有简称、映射测试。

- [x] 失败测试：声明全称、简称保存后重载应逐处还原；两个机构共用“小麦”时分别编号；原文含 [公司1] 时不能碰撞。

```python
全称代号 = 映射.注册机构称呼("E1", "成都星河科技有限公司", "公司", 是否全称=True)
简称代号 = 映射.注册机构称呼("E1", "星河", "公司")
self.assertEqual(全称代号, "[公司1]")
self.assertEqual(简称代号, "[公司1]①")
映射.保存到文件(映射路径)
重载 = 全局映射表(); 重载.从文件加载(映射路径)
self.assertEqual(重载.批量还原文本("[公司1]与[公司1]①"), "成都星河科技有限公司与星河")
```

- [x] 最小实现：version=2 的 JSON 保存机构、每种称呼、唯一代号及必要原值类型和公式；载入时兼容旧平面映射。旧数据重复代号的歧义如实保留记录，不编造原称呼。保留现有圈号分配逻辑，修复显式简称合并绕过该机制的问题。
- [x] 核心约束：反向映射[代号] = 原文；机构关系独立存储。多个机构同名时正向映射不能强行取单一代号，正式写回只读取已定位的机构编号与称呼。

```python
# 注册机构称呼中先复用当前机构的原写法；机构编号来自本文件识别结果。
机构 = self._机构[机构编号]
已有代号 = 机构["称呼"].get(原文)
if 已有代号 is not None:
    return 已有代号
# 分配完成后保存，不能把另一个原词覆盖到同一代号。
机构["称呼"][原文] = 代号
self._反向[代号] = 原文
self._版本 += 1
```

映射类增加 _机构 字典；其记录含主代号、称呼字典和下一个圈号序号。测试开始用 全局映射表() 构造 映射；圈号分配在现有逻辑上扩展，超过现有圈号范围时使用无碰撞的序号写法并验证还原。
- [x] 验证：--module 开发测试.test_机构识别改进 --module 开发测试.test_简称验证 --module 开发测试.test_ner引擎；包括超过十个原文写法及重载后继续分配。

## 任务 4：模型结构解析与请求错误分别处理

修改 [NER][NER]，复用 [现有接口配置][桥接]；测试 [机构测试][机构测试]及现有在线接口测试。

- [x] 失败测试：思考混入 content、围栏 JSON、非法实体引用、截断输出各有明确结果；超时可有限重试，401 不重试；有效空结果区别于请求失败。

```python
消息 = {"content": '</think>\n{"entities":[],"mentions":[]}'}
结果 = 引擎._解析文档返回(消息, 块列表)
self.assertEqual(结果.出现, [])
self.assertEqual(结果.未完成, [])
错误结果 = 引擎._解析文档返回({"content": '{"entities":['}, 块列表)
self.assertTrue(错误结果.未完成)
```

- [x] 最小实现：新增 _解析文档返回(消息, 块列表) -> 识别结果；验证完整最终 JSON、字段类型、块号、原词、机构引用和原文证据。保持旧数组解析用于旧接口兼容，不从思考草稿中拼出“成功结果”。请求异常保留类别和位置，超时/临时连接/429/可恢复服务错误额外至多两次；401/403/确定配置错误不原样重试。
- [x] 输出截断先调整请求预算或分块；结构错误提供明确纠正信息；记录输入覆盖与失败块。日志默认不输出密钥和全文。

```python
# 解析层只在验证结构后承认覆盖；调用层另行处理请求异常。
if not isinstance(数据, dict) or not isinstance(数据.get("entities"), list) or not isinstance(数据.get("mentions"), list):
    return 识别结果({}, [], set(), [{"环节": "返回结构", "原因": "缺少合法 entities 或 mentions"}])
if any(not isinstance(item, dict) for item in 数据["entities"] + 数据["mentions"]):
    return 识别结果({}, [], set(), [{"环节": "返回结构", "原因": "列表项目必须是对象"}])
```

数据来自现有正式回答抽取后的一次 JSON 解析；单个条目继续校验原文和引用，未通过的条目不进入已核对范围。截断/解析失败不与合法空列表走同一返回分支。
- [x] 验证：--module 开发测试.test_机构识别改进 --module 开发测试.test_ner引擎_在线模式 --module 开发测试.test_在线api函数；断言真实返回内容及请求次数，不能只检查 mock 被调用。

## 任务 5：两轮识别与全文机构关系

修改 [NER][NER]，使用 [原文块][结果]；测试 [机构测试][机构测试]及现有跨块测试。

- [x] 失败测试：简称在前、全称及关系在后；无全称简称；母子公司；同名不同主体；第二轮发现首轮漏项；“小麦（粮食）”保留。用固定回复仅验证流程，真实质量另由任务 12 验证。

```python
结果 = 引擎.识别文档(先简称后全称的块)
self.assertEqual(结果.已核对块, {x.编号 for x in 先简称后全称的块})
self.assertTrue(any(m.原词 == "小麦" and m.块编号 == "运输" for m in 结果.出现))
self.assertFalse(any(m.块编号 == "粮食" for m in 结果.出现))
self.assertNotEqual(母公司编号, 子公司编号)
```

- [x] 最小实现：首轮遍历全文件产生机构与关系证据，第二轮对全部块提供机构资料、表头和邻文。已知名称不再要求跳过；任意新简称不受两字、英文、后缀或黑名单门槛。旧“去后缀即自动登记”和包含即归父实体改为候选证据。
- [x] 调用骨架：机构资料 = 首轮发现(块列表)；结果 = 第二轮核对(块列表, 机构资料)。两者均为 NER引擎 内部方法，不新增独立调度框架。名单过长分组核对全部组，输入块和结果预算分别保留，不截断后文。

```python
# NER引擎.识别文档 的顺序。首轮发现返回机构资料与未完成记录。
机构资料, 首轮未完成 = self.首轮发现(块列表, 进度回调)
结果 = self.第二轮核对(块列表, 机构资料, 进度回调)
结果.未完成.extend(首轮未完成)
return 结果
```

首轮发现(块列表, 进度回调=None) → tuple[dict, list[dict]]；第二轮核对(块列表, 机构资料, 进度回调=None) → 识别结果。两轮内部使用任务 4 的结构校验，逐块记录处理情况。上述测试中“运输”块为“向小麦支付运费”，后文定义“小麦系青岛禾舟运输有限公司的简称”，“粮食”块为“采购小麦（粮食）”；母子公司编号分别从结果中两个明确全称对应的记录取得，不能预设相同或不同编号冒充测试。
- [x] 验证：--module 开发测试.test_机构识别改进 --module 开发测试.test_ner引擎_跨块复用 --module 开发测试.test_简称验证。调整与新需求冲突的旧测试时说明行为变化，保留其他原有覆盖。

## 任务 6：逐处定位、重叠处理与覆盖核对

修改 [识别结果][结果]和 [NER][NER]；测试 [机构测试][机构测试]。

- [x] 失败测试：同词两次一处机构一处普通词，只替换机构位置；多个相同摘录不能任取第一处；已列机构却未返回出现位置时能发现覆盖差异。

```python
原文 = "向小麦付款，采购小麦（粮食）。"
开始 = 原文.index("小麦")
self.assertEqual(按位置替换(原文, [(开始, 开始 + 2, "[机构1]")]), "向[机构1]付款，采购小麦（粮食）。")
with self.assertRaises(ValueError):
    按位置替换(原文, [(0, 4, "甲"), (2, 5, "乙")])
```

- [x] 最小实现：先验证原词切片一致，统一合并重叠候选，再从后往前应用；完整全称优先于内嵌简称。摘录不唯一时枚举本地位置编号，附上下文定向核对。记录“已确认机构、普通用法、待核对”的位置，已知词匹配仅产生候选，不触发全局替换。
- [x] 原文归一只发生在识别副本，保留字符对应，特别验证全角、组合空白及跨行位置。对尚未入名单的简称仍依靠开放识别，不能宣称字面覆盖核对已排除全部遗漏。

```python
def 按位置替换(原文: str, 替换: list[tuple[int, int, str]]) -> str:
    有序 = sorted(替换)
    上次结束 = 0
    for 开始, 结束, 代号 in 有序:
        if not 0 <= 开始 < 结束 <= len(原文) or 开始 < 上次结束:
            raise ValueError("替换位置越界或重叠")
        上次结束 = 结束
    for 开始, 结束, 代号 in reversed(有序):
        原文 = 原文[:开始] + 代号 + 原文[结束:]
    return 原文
```
- [x] 验证：--module 开发测试.test_机构识别改进；同时测位置越界、原词不一致、相邻及嵌套命中。

## 任务 7：其他敏感类别、词典与成熟规则

新增 [固定规则][规则]、[规则测试][规则测试]、[来源记录][来源]，修改 [NER][NER]、[词典][词典]与 [依赖][依赖]。

- [x] 失败测试：手机号空格/横线/全角、括号区号与分机、国际号码、银行账号数字及文本、信用代码大小写及校验、身份证分类冲突；人名和地址的上下文用例放入机构测试及真实标注。金额及日期开关两种状态均有断言。

```python
块 = 文本块("t1", "联系电话：138 0013 8000；物料编号：ABCDEFGHIJKLMNOPQR", {})
命中 = 固定规则识别(块, 启用日期=False, 启用月日=False, 启用金额=False)
self.assertTrue(any(m.原词 == "138 0013 8000" and m.类型 == "mobile_phone" for m in 命中))
self.assertFalse(any(m.原词 == "ABCDEFGHIJKLMNOPQR" and m.类型 == "credit_code" for m in 命中))
```

- [x] 批准后只安装 phonenumbers==9.0.39；记录 Apache-2.0 来源和版本。以 CN 为默认地区并支持显式国际区号，配合中文电话标签；银行卡校验不套用于全部对公账户。标识符校验失败但字段明确时保留敏感候选，不能直接放过。
- [x] 最小实现：把固定格式识别从 NER 主流程中独立成一个模块，返回出现记录；使用已有地名、行业词和法律形式做候选边界辅助。邮箱、车牌、IPv4/MAC、金额、日期全部建正反例，中文日期及金额参考上游用例，纠正币种变化。

```python
import phonenumbers
# 规则入口中的电话分支；其他类型保留现有类别名。
命中 = []
for 匹配 in phonenumbers.PhoneNumberMatcher(块.原文, "CN"):
    开始, 结束 = 匹配.start, 匹配.end
    号码类型 = phonenumbers.number_type(匹配.number)
    类型 = "mobile_phone" if 号码类型 == phonenumbers.PhoneNumberType.MOBILE else "landline"
    命中.append(出现记录(块.编号, 开始, 结束, 块.原文[开始:结束], 类型, 来源="规则"))
```

与现有类型映射核对后保持其外部名称，必要时在本模块转换。全角或格式归一必须带原文字符映射，不能直接把归一后的偏移用于写回。分机作为同一敏感跨度保存。
- [x] 验证：--module 开发测试.test_固定规则 --module 开发测试.test_ner引擎 --module 开发测试.test_复核修复；逐条记录移植来源文件与许可，未核实 AuditMask 来源不冒充已确认。

## 任务 8：Word、PPT、TXT 和 MD 按位置写回

修改 [写回][写回]和 [Word工具][Word工具]；测试 [输出测试][输出测试]及现有格式测试。

- [x] 失败测试：跨两个样式片段的机构只替换一次，普通词同形保留，未命中文字格式不变；正文、表格、当前支持的页眉页脚均覆盖，MD 结果与原格式用同一位置判断。

```python
输出路径 = 保存定位脱敏(str(原文件), 块列表, 识别结果, 映射, str(输出目录))
重新读取 = 读取文档块(输出路径)
self.assertIn("[公司1]①", "\n".join(x.原文 for x in 重新读取))
self.assertIn("联合开展研发", "\n".join(x.原文 for x in 重新读取))
self.assertEqual(未命中片段的字体属性, 修改前字体属性)
```

- [x] 最小实现：构建段内字符到文本节点的对应，只改目标跨度，把代号写入首个命中片段并清除该跨度后续文字；其余片段文字和样式保留。全称、简称按任务 3 的唯一代号还原；不用整段按长度比例重新分摊。
- [x] MD 从已定位的各原文块生成，不能重新拿词典全局替换；已有扫描 MD 路径复用同一处理。

```python
# 单次命中跨度已换算到节点内部偏移；从后往前处理多个命中。
if 首节点 is 尾节点:
    首节点.text = 首节点.text[:首偏移] + 代号 + 首节点.text[尾偏移:]
else:
    首节点.text = 首节点.text[:首偏移] + 代号
    for 节点 in 中间节点:
        节点.text = ""
    尾节点.text = 尾节点.text[尾偏移:]
```

Word 节点为 w:t，PPT 节点为 run；先由任务 2 的位置累计各节点文字长度取得首尾节点与偏移，不改节点样式属性。测试字体属性从生成的样例及重开输出中读取，不能只比较内存里的原变量。
- [x] 验证：--module 开发测试.test_输出完整性 --module 开发测试.test_格式保持器；重开、保存映射、重载还原三步齐全，并检查 Word/PPT 样式与版面截图。

## 任务 9：Excel 值、公式与还原

修改 [解析器][解析]和 [写回][写回]，扩展 [NER 中映射][NER]；测试 [输出测试][输出测试]。

- [x] 失败测试：数字型号码可脱敏并恢复原类型；公式引用 ='星河'!A1 不被按简称改成不存在的工作表；无缓存的 ="星"&"河" 实际结果参与识别；普通 SUM 公式不变。

```python
self.assertEqual(输出工作簿.active["B1"].value, "='星河'!A1")
self.assertEqual(输出工作簿.active["D1"].value, "=SUM(1,2)")
self.assertEqual(还原工作簿.active["E1"].value, '=\"星\"&\"河\"')
self.assertIsInstance(还原工作簿.active["B2"].value, int)
```

- [x] 最小实现：原值、公式和显示值分别读取；缺缓存或需要重新计算时通过独立隐藏 Excel 实例读取，原文件只读、不更新外部链接。只操作任务工作簿，finally 关闭该实例。
- [x] 公式常量可安全定位时只改其常量；公式计算出的敏感内容无法安全局部改动时，仅该命中单元格写入脱敏值，并保存原公式/类型/坐标。普通公式不改；还原时恢复原公式。引用已处理源单元格的公式优先保持引用，让计算结果自然使用代号。

```python
# 仅用于确定无法安全保留公式的敏感命中单元格，记录进入 version=2 映射。
原值记录 = {"工作表": 工作表.title, "单元格": 单元格.coordinate,
            "原值": 单元格.value, "数据类型": 单元格.data_type}
单元格.value = 按位置替换(实际显示文本, 本格替换)
# 还原分支在完成代号还原后，按精确位置恢复原值及类型。
还原单元格.value = 原值记录["原值"]
还原单元格.data_type = 原值记录["数据类型"]
```

Excel 实例由 DispatchEx 创建；Workbooks.Open 使用 ReadOnly=True、UpdateLinks=0，读取 Range.Value2。仅在任务输出副本上保存计算缓存，原件保持只读。B1/C1/D1/E1 的公式与本轮已保留的真实探查样例相同；B2 另放数字型手机号。
- [x] 验证：--module 开发测试.test_输出完整性 --module 开发测试.test_文档解析器；再用实际 Excel 核对显示、引用、公式恢复和原文件哈希。无可用计算器或外部数据缺失时记录该位置未完整，不拦截其他结果交付。

## 任务 10：PDF 保存和位置还原

修改 [写回][写回]，必要时复用 [解析器][解析]的文字位置；测试 [输出测试][输出测试]。

- [x] 失败测试：当前同路径保存报错已复现；新输出可打开、目标原文消失、代号存在，同词普通用法不被全页 search_for 一起删除。

```python
with pymupdf.open(输出PDF) as doc:
    文本 = "\n".join(page.get_text() for page in doc)
self.assertNotIn("13800138000", 文本)
self.assertIn("[手机号1]", 文本)
self.assertIn("联合开展研发", 文本)
```

- [x] 最小实现：按核准区域移除和写入，普通保存到新路径，清理无用对象；不向已打开的源路径普通保存，不用增量保存规避该错误。保留必要坐标、字体和原文写法供还原，检查代号及还原文字是否容纳，不能隐藏截断。

```python
import pymupdf
with pymupdf.open(原PDF) as doc:
    # 已核准区域来自具体出现记录，不对全页同名文字批量搜索。
    for 页号, 矩形, 代号 in 已核准区域:
        doc[页号].add_redact_annot(矩形, text=代号, fontname="china-s")
    for page in doc:
        page.apply_redactions()
    doc.save(新输出PDF, garbage=4, deflate=True)
```

上述为保存算法，坐标、字号和容纳情况要经实际渲染检查；中文代号、圈号及长原文还原均有样例。输出路径必须与打开路径不同。
- [x] 验证：--module 开发测试.test_输出完整性 --module 开发测试.test_格式保持器；重开读取与实际页面渲染都检查。扫描 PDF 仍按已有 OCR→MD 路径验收，不把它声称为扫描 PDF 原版面还原。

## 任务 11：完整处理入口、残留检查与异常交付

修改 [处理器][处理器]、[界面][界面]与 [NER][NER]；测试 [输出测试][输出测试]。

- [x] 失败测试：模拟一个识别块超时，已识别处仍生成文件与映射，界面说明哪些环节未完成；不会把文件同时登记为全部成功和失败。模型已有一家公司时仍补漏其他位置。取消后不再发起新的模型请求。

```python
结果 = 处理器.处理文件列表([str(原文件)], str(输出目录))
self.assertTrue(结果.已生成文件)
self.assertTrue(all(Path(p).exists() for p in 结果.映射表路径列表))
self.assertTrue(结果.未完成环节)
self.assertEqual(结果.成功数, 0)
```

- [x] 最小实现：结果增加 已生成文件 和 未完成环节；成功数只对应处理完整的文件，但任何已生成结果都可取得。先保存匹配的映射与输出，再更新记录，恢复部分失败时沿用已确认代号。批处理按文件继续，故障不导致完成文件白做。
- [x] 保存后重读：检查实际替换数量和原文跨度、固定规则残留、模型开放式寻找遗漏；已判普通用法的同形词不报残留。发现漏写修写回，发现漏识别补上下文；不原样重复正则。每个定向问题先补充核对一次，新证据才触发相关位置再核对。

```python
# 仅在实际文件与对应映射都保存成功后登记；出现部分异常仍保留路径。
结果.已生成文件.append(输出路径)
结果.映射表路径列表.append(映射路径)
if 本文件未完成:
    结果.未完成环节.extend(本文件未完成)
else:
    结果.成功文件.append(输出路径)
```

沿用现有结果类的 成功文件、失败文件；新增 已生成文件、未完成环节 均为列表。失败文件仅记录未能产生可用结果的文件，避免把同一文件重复计数。复查使用原始上下文和保存后的定位对应，不能因代号替换改变语义而误判普通词。
- [x] 验证：--module 开发测试.test_输出完整性 --module 开发测试.test_机构识别改进 --module 开发测试.test_复核修复。实际窗口检查进度、结果路径和映射路径；文件选择与取消检查按钮调用链，保留现有使用入口。

## 任务 12：真实模型、完整格式验收、审查与交付

新增 [标注数据][标注]及 [真实验证脚本][模型验证]，更新 [说明][说明]和 [来源记录][来源]。

- [x] 失败测试先检查评测器：故意漏一处、误加一处和错连一家机构，必须分别报告，不以检出名称总数抵消错误。

```python
分数 = 核对标注(标准出现, 故意漏检误检及错连的结果)
self.assertEqual(分数["漏检"], 1)
self.assertEqual(分数["误检"], 1)
self.assertEqual(分数["错误关联"], 1)
```

- [x] 最小实现：验证脚本内部 核对标注 按文件、块、位置、类型、主体逐项比较。样例在模型调用前固定，覆盖明示和未明示简称、同词普通用法、英文、无全称、跨块与前后倒序、多机构同名、表格关系、人名地址及固定格式；每份输出保存重载还原。

```python
def 核对标注(标准出现: list[dict], 实际出现: list[dict]) -> dict:
    字段 = ("文件", "块编号", "开始", "结束", "类型")
    标准 = {tuple(x[k] for k in 字段): x["主体"] for x in 标准出现}
    实际 = {tuple(x[k] for k in 字段): x["主体"] for x in 实际出现}
    return {"漏检": len(标准.keys() - 实际.keys()),
            "误检": len(实际.keys() - 标准.keys()),
            "错误关联": sum(标准[k] != 实际[k] for k in 标准.keys() & 实际.keys())}
```

执行评测前验证相同位置没有重复相互冲突记录；主体按有证据的全称或标注的独立机构对应，不能直接比较两个版本任意分配的编号。核心失败样例包含一个只在标准中的位置、一个只在结果中的位置和一个同位置错连主体，分别应为 1。
- [x] 对任务 1 改前基线和新版本运行相同原文、同一 Qwen3.8-27B 接入和相同可比较参数，记录输入输出令牌、调用量、耗时、漏检、误检、错连及文件写回结果。密钥经会话输入，不保存到样例、命令日志或报告。模型只有一次简单样例通过不能代替长文和多格式验证。
- [x] 使用指定解释器运行 模型验证脚本 的 --baseline 与 --current 模式；参数用 Windows 文件选择窗口或内部已知工作路径配置，不要求用户手填路径。原始样例仅虚构；报告列明未通过项，不伪造准确率和耗时结论。
- [x] 完成 --all、AST/导入检查、git diff --check，以及对应文件重开、版面检查和实际 GUI 入口验证。检查每项设计要求都有结果或已修复失败，不做无关仓库全量检查。
- [x] 阶段 5 按技能内置审查和完成前验证指引串行复核：查源码泄露密钥、遗漏格式、旧映射兼容、原文保护、错误归并、逐文件隔离及输出状态。修复实际问题后仅重跑受影响验证，最后完成项目汇总。
- [x] 阶段 6 按完整工作流交付修改位置、测试结果、模型对比、规则来源和实际输出样例；依据后续用户明确授权，已提交推送 main（3e7bac9），没有发布安装包。

## 任务依赖与计划自检

任务 1→2→3→4→5→6 建立机构主线；任务 7 补齐其他类别；任务 8、9、10 将识别落到所有当前格式；任务 11 收拢实际入口及复查；任务 12 完成真实质量和整体交付。全部串行执行，不在每个小步骤后请求用户确认。

覆盖检查：方案第 1—4 节对应基线及任务 5、12；第 5 节对应任务 2—6；第 6 节对应任务 7、12；第 7 节对应任务 8—11；第 8 节对应每项测试和任务 12；第 9 节对应任务 1、3、12。用户两个 A 及两次异常处理纠正均进入对应任务。当前没有已知缺失的范围项。

本计划批准内容：在当前专用克隆内建立本地实施分支、修改上述源码与测试、安装一个缺失的 phonenumbers 包、使用已有本机 Excel 做任务文件计算验证、调用指定模型进行虚构材料评测。批准后连续执行阶段 4、5、6 的已授权工作；遇到实际新增业务取舍才说明，不追加假设性的选择题。

主要风险及验证：模型可能漏掉未明示简称，以固定标注的逐处漏检、误检和错连比较检验；定位错误可能误改普通词，以同词不同用途及保存后重读检验；Excel 公式与 PDF 文字布局可能损坏，以实际 Excel 重算、公式还原、PDF 重开和页面渲染检验；新映射可能破坏旧数据，以旧格式加载及新格式保存重载还原检验。实际结果见执行记录及验收审查；有限样例不代表所有文档零遗漏，PDF 字体版面及真实扫描 OCR 准确率边界已披露。

[设计]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\docs\superpowers\specs\2026-09-18-机构简称与全面脱敏改进-design.md>
[需求]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\docs\requirements\2026-09-18-机构简称与全面脱敏改进-requirement.md>
[代号决策]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\docs\adr\0001-机构关联与原文写法分别保存.md>
[结果]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\识别结果.py>
[解析]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\文档解析器.py>
[Word工具]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\_docx_xml工具.py>
[NER]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\ner引擎.py>
[规则]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\固定规则.py>
[词典]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\词典.json>
[写回]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\格式保持器.py>
[处理器]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\脱敏处理器.py>
[界面]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\app.py>
[桥接]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\主程序\mineru桥接.py>
[验证入口]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\维护工具\运行脱敏验证.py>
[机构测试]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\开发测试\test_机构识别改进.py>
[输出测试]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\开发测试\test_输出完整性.py>
[规则测试]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\开发测试\test_固定规则.py>
[标注]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\开发测试\样例\机构识别标注.json>
[模型验证]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\维护工具\验证机构识别.py>
[依赖]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\requirements.txt>
[来源]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\docs\规则来源.md>
[说明]: <C:\Users\27651\Documents\Codex\2026-09-18\https-github-com-silent-quills-datamask\work\local-doc-desensitizer\README.md>

## 实际实现及验收补充

内部实现分为文档识别.py、定位写回.py，既有模块保留调用入口。两轮开放识别后，对机构简称每次出现按编号和上下文核对，包括模型已肯定的位置；这一补充来自实际 Word 普通词误判的复现与修复。PPT 读写统一到文字节点，保留软换行。没有增加逐词人工确认。

最终离线 112 项通过；真实模型 3 份标注共 36 处、漏检/误检/错连均 0，保存重载准确还原。实际 Word 全流程通过；Office 和 PDF 已查看渲染，GUI 已通过实际启动器检查。详细证据在《实施交付记录》及 docs/reviews/2026-09-18-机构简称与全面脱敏改进-review.md。

本地可用代码、启动入口、样例和说明已经交付。后续 Excel 优化新增 12 项回归，共 124 项通过；其需求、方案、计划及审查另行记录。已按用户选择在现有 main 提交推送，功能提交 3e7bac9 已核对远端一致；此前工作分支仅保留，没有继续在其上实施。
