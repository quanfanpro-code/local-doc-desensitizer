# 独立复核 — 验收审查报告（2026-09-03）

对应需求：docs/requirements/2026-07-21-在线api调用-requirement.md + docs/requirements/2026-07-25-全面复核-requirement.md
对应计划：docs/superpowers/plans/2026-07-21-在线api调用.md + docs/superpowers/plans/2026-07-25-全面复核修复.md

## 审查范围

今日合并后的代码：简称声明合并（新）、LLM 超时 600s（改）、settings.json 密钥隔离（改）、云端修复带回（合并）。逐项对照需求文档验收标准，并做正确性、安全性、过度工程审查。

## 修复核对（逐项对照）

| # | 需求/问题 | 状态 | 验证方式 |
|---|-----------|------|----------|
| 1 | 同一公司被拆成两个代号（[公司3]/[公司6]） | ✅ 已修 | 红绿验证：旧版失败（简称被加圈数字后缀），新版 8 项全绿 |
| 2 | LLM 超时 6 分钟 → 10 分钟 | ✅ 已改 | ner引擎.py 两处 timeout 已改为 600 |
| 3 | settings.json 密钥隔离 | ✅ 已改 | 移出版本管理 + .gitignore；推送前密钥扫描为 0 |
| 4 | 调试日志默认关闭（云端带回） | ✅ 已确认 | 脱敏处理器.py 74 行 `debug_ner_log` 开关已生效 |
| 5 | 扫描版 PDF 空白输出（云端带回） | ✅ 已确认 | 生成脱敏md 传入原始文本，不再重新提取 |
| 6 | PDF 还原排序与图片保护（云端带回） | ✅ 已确认 | 格式保持器.py 反向映射按长度排序 + PDF_REDACT_IMAGE_NONE |
| 7 | 环境自检 ltp → ahocorasick（云端带回） | ✅ 已确认 | 维护工具/环境自检.py 已更新 |
| 8 | 后台线程读界面变量崩溃隐患（云端带回） | ✅ 已确认 | app.py 主线程先取值再传入后台线程 |
| 9 | 主程序直接运行支持（云端带回） | ✅ 已确认 | app.py 包导入兜底已加 |
| 10 | 简称超 10 个静默放弃泄露（云端带回） | ✅ 已确认 | 注册自定义映射 圈数字用完后自动改用数字后缀 |

## 审查发现与修复

| 严重度 | 位置 | 问题 | 处理 |
|--------|------|------|------|
| 🔴 | ner引擎.py 注册自定义映射 | 简称声明合并时“未登记简称挂靠”会触发碰撞逻辑，简称被加圈数字后缀而非共用主代号 | 已修：加 `允许共用` 参数，简称合并场景允许多原文共用同一代号 |
| 🟡 | ner引擎.py | `反向查找()`、`导出()` 零调用，死代码 | 已删（-7 行） |
| 🟢 | ner引擎.py _合并简称声明 | 机构标签防护、幂等保护、长度降序匹配均已确认完备，无过度设计 | 无需改动 |

## 红绿验证

- **红**：用审查前备份验证旧版，场景2（未登记简称挂靠）确实失败——“本所”被注册为 `[合伙企业1]①` 而非 `[合伙企业1]`
- **绿**：修复后 8 项验证全部通过（含防误挂、幂等、脱壳变体并入、无声明不新增）
- **全量**：74 个测试全部通过（原有 68 + 云端带回复核测试 6）

## 密钥安全

- 推送前扫描：`git diff --cached | grep -c '<新密钥前缀>'` = 0；`<旧密钥前缀>` = 0
- settings.json 已从版本库移除（`git rm --cached`），本地文件完好
- .gitignore 已加入 settings.json、__pycache__、*.pyc、*.log、*.baiduyun.uploading.cfg 等

## 过度工程自查（ponytail）

- 新增 `合并代号` 方法：简称声明合并的核心依赖，必须保留
- 新增 `允许共用` 参数：修复真实 bug 所需，非过度设计
- 删除 2 个零调用方法：`反向查找()`、`导出()`，净减 7 行
- 无新增依赖、无新类、无投机性抽象

## 兼容性

- 映射表文件格式未变，旧映射表仍可正常还原
- `注册自定义映射` 新参数为可选（默认 False），旧调用方行为不变
- 合并简称声明仅在识别到“（以下简称××）”句式时触发，无声明时零开销

## 遗留说明（非 bug）

- settings.json 里 API 密钥为明文存储，属既有设计；本次已将其移出版本管理，消除上传泄露风险
- 合并简称声明依赖文档中出现“（以下简称××）”句式；若文档仅用“以下简称××”（无括号），本机制不触发，退回原有父实体匹配逻辑

## 验证证据

- 红验证：旧版 ner引擎_审查前.py 场景2 失败（本所代号 ≠ 主代号）
- 绿验证：新版 8 项全绿
- 全量测试：`python -X utf8 -m pytest 开发测试 -q` → 74 passed
- 密钥扫描：0 次出现
