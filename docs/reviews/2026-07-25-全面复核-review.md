# 全面复核 — 验收审查报告（2026-07-25）

对应需求：docs/requirements/2026-07-25-全面复核-requirement.md
对应计划：docs/superpowers/plans/2026-07-25-全面复核修复.md

## 修复核对（需求清单 1~11）

| # | 问题 | 状态 | 验证方式 |
|---|------|------|----------|
| 1 | 扫描版PDF脱敏后 md 空白 | ✅ 已修 | `test_传入文本时直接用它`（故意给不存在的文件路径，证明不再重新提取） |
| 2 | 调试日志泄露原文 + 句柄泄漏 | ✅ 已修 | `test_默认不产生调试日志` / `test_开启后生成且句柄被关闭` / `test_开启后处理抛异常句柄也被关闭` |
| 3 | 在线模式误查本地 LM Studio | ✅ 已修 | app.py `start_desensitize` 改用 `_探测当前llm状态()`，目检 |
| 4 | 扫描版PDF前提应查 MinerU | ✅ 已修 | app.py 改查 `默认ocr引擎.是否可用()`，目检 |
| 5 | 进度条 0~100 越界 | ✅ 已修 | 去掉 `*100`，目检 |
| 6 | 映射碰撞超 10 次静默放弃 | ✅ 已修 | `test_超过10次碰撞仍然全部注册` |
| 7 | 直接运行 app.py 导入崩溃 | ✅ 已修 | `cd 主程序 && python -c "import app"` 通过 |
| 8 | 环境自检 ltp 过时 | ✅ 已修 | 运行 `维护工具/环境自检.py` 全部 OK |
| 9 | 后台线程读 Tkinter 变量 | ✅ 已修 | 变量改为主线程先取值再传入，目检 |
| 10 | NER 警告文案不分模式 | ✅ 已修 | 按 llm_mode 区分文案 |
| 11 | 死代码清理 | ✅ 已修 | 删未使用变量、re 提升到模块顶部 |

## 测试

- 命令：`python -m pytest 开发测试 -q`
- 结果：**62 passed**（原有 56 + 新增 6），无失败
- 新增测试覆盖：正常路径 + 边界（不存在文件、异常抛出、超上限碰撞）+ 默认行为保持

## 过度工程自查（ponytail）

- 未新增任何依赖、任何新类；调试日志开关复用现有 settings.json 机制
- 直接运行修复只在入口 app.py 加了两行路径插入，没有给每个子模块加兜底

## 兼容性

- 映射表文件格式未变，旧映射表仍可正常还原
- `生成脱敏md` 新参数为可选，旧调用方行为不变（有测试锁定）

## 遗留说明（非 bug）

- settings.json 里 API 密钥为明文存储，属既有设计；readme 已提示在线 API 会产生费用，密钥本身未做改动
- readme.md 已同步更新（扫描版PDF前提、调试日志开关、更新记录新条目）
